<div id="people-recap-{{people.id}}" data-people-id="{{people.id}}" class="col-md-6 mt-3">
    <div class="border p-3 h-100">
        <div class="row">
            <div class="col-3">
                <img class="img-thumbnail" src="{{ asset('images/placeholder-personne.jpg') }}" alt="Profile photo placeholder">
            </div>
            <div class="col-7">
                <div class="row"> <!--row name -->
                    <div id="people-recap-name-{{people.id}}" class="col">
                        {{ people.denomination ? people.denomination.label : "" }} {{ people.firstname }} {{ people.lastname | upper }}
                    </div>
                </div>
                <div class="row"> <!--row address -->
                    {% if not people.hasNoAddressDefined() %}
                        {% for address in people.addresses %}
                        <div class="col-sm-12">
                            <div class="row">
                                <div class="col">
                                    {{address.line}}
                                </div>
                            </div>
                            {% if not address.lineTwo is null %}
                                <div class="row">
                                    <div class="col">
                                        {{address.lineTwo}}
                                    </div>
                                </div>
                            {% endif %}
                            <div class="row">
                                <div class="col">
                                    {{address.postalCode}} {{address.city}}
                                </div>
                            </div>
                            <div class="row">
                                <div class="col">
                                    {{address.country}}
                                </div>
                            </div>
                        </div>
                        {% endfor %}
                    {% else %}
                    <div class="col">
                        Pas d'adresse enregistrée.
                    </div>
                    {% endif %}
                </div>
            </div>
            <div class="col-2 text-right">
                <button
                    type="button"
                    class="btn btn-secondary"
                    onclick="handlePeopleDeletion({{people.id}})"
                    title="{% trans %}Retirer la personne de la nouvelle adhésion{% endtrans %}"
                >
                    <i class="icon ion-md-trash"></i>
                </button>
            </div>
        </div>
    </div>
</div>